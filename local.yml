- hosts: localhost
  become: true
  tasks:
  - name: install vscode snap with option --classic
    snap: 
      name: code
      classic: yes
  - name: install packages
    apt: name={{item}}
    with_items:
      - python3-pip
      - pipenv
